This directory contains files that are included via the #include <file.h>
preprocessor directive.

net/		network related include files
lib/		directory for .c files, so code can be kept out of .h files

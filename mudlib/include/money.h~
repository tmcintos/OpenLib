#ifndef _MONEY_H
#define _MONEY_H

#define COIN_TYPES     2              // size of array used to pass money
#define SILVER         0              // index of silver coin in array
#define GOLD           1              // index of gold coin in array

#define ONE_GOLD_IN_SILVER 50         // how many silver make 1 gold

#endif /* _MONEY_H */

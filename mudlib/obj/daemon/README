This directory is for objects that are intended to only have a _single_
instance (no clones) yet are not typically inherited.  This sort of object
includes what are sometimes known as daemons.

Daemons which must be secure should _not_ be placed here.  They belong in
the secure daemon directory.

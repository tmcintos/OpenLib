.\"directory_exists() - determine whether or not a directory exists
.TH directory_exists 2 "Oct 18 1995" Ultralib "Simulated Efuns"

.SH NAME
directory_exists() - determine whether or not a directory exists

.SH SYNOPSIS
int directory_exists( string dirname );

.SH DESCRIPTION
directory_exists() returns 1 if the directory given by the full-path 'dirname'
exists, and returns 0 if the directory does not exist.

.SH SEE ALSO
file_exists(2), file_size(3)

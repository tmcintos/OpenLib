.\"log_file() - log a diagnostic message to a file in the /log directory
.TH log_file 2 "Oct 18 1995" Ultralib "Simulated Efuns"

.SH NAME
log_file() - log a diagnostic message to a file in the /log directory

.SH SYNOPSIS
void log_file( string filename, string message );

.SH DESCRIPTION
log_file() appends the string in 'message' along with a timestamp to the
file named in 'filename' which is a path relative to the /log directory.

.SH SEE ALSO

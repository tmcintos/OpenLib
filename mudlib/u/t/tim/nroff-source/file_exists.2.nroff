.\"file_exists() - determine whether or not a file exists
.TH file_exists 2 "Oct 18 1995" Ultralib "Simulated Efuns"

.SH NAME
file_exists() - determine whether or not a file exists

.SH SYNOPSIS
int file_exists( string filename );

.SH DESCRIPTION
file_exists() returns 1 if the file given by the full-path 'filename' exists,
and returns 0 if the file does not exist.

.SH SEE ALSO
file_size(3), directory_exists(2)

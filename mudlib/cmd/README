/cmd:  'insecure' Command Directory.
       This directory is to contain the objects implementing the commands that
       interactives can use (movement, building, communication, etc).

./adm           Administrative commands.
./player        Player commands.
./wiz           Wizard commands. (cmds which write files should go under /adm)
./verbs         Verb handlers for the MudOS command parser
